site_name: Distributed

nav:
  - Home: index.md
  - Velocity class: velocity.md
  - Level class: level.md
  - Reinit class: reinit.md

theme:
  name: material
  palette:
    - media: "(prefers-color-scheme: dark)"
      scheme: slate
      primary: cyan
      accent: light blue
    - media: "(prefers-color-scheme: light)"
      scheme: default
      primary: deep purple
      accent: amber

markdown_extensions:
  - admonition
  - toc:
      permalink: true
  - pymdownx.arithmatex:
      generic: true

extra_javascript:
  - https://cdn.jsdelivr.net/npm/mathjax@3/es5/tex-mml-chtml.js

plugins:
  - search
  - mkdocstrings:
      default_handler: python
      handlers:
        python:
          options:
            docstring_style: numpy
            show_root_full_path: false
            show_source: true


